# Environment variables for Gidit Desktop
# These values are used at runtime to configure the application.

# Path to the SQLite database (relative to the Electron userData directory)
DB_PATH=gidit.db

# Default Ollama model name used by the AI layer
OLLAMA_MODEL=gemma3-270m

# Location of the portable Python runtime.  When packaging the app this
# folder is copied into the final build.  During development the Python
# scripts run from the `python/` directory.
PYTHON_RUNTIME_PATH=resources/python-runtime
